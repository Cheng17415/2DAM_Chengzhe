BBDD - MySQL

Código para crear la estructura con datos en MySQL: 

CREATE SCHEMA ecommerce;
USE ecommerce;

CREATE TABLE rol (
    rol_id INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(50) NOT NULL UNIQUE
);

CREATE TABLE usuario (
    usuario_id INT AUTO_INCREMENT PRIMARY KEY,
    dni VARCHAR(9) UNIQUE,
    nombre VARCHAR(50) NOT NULL,
    apellido VARCHAR(50) NOT NULL,
    direccion VARCHAR(60),
    codigo_postal VARCHAR(10),
    telefono VARCHAR(20),
    email VARCHAR(70) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    rol_id INT NOT NULL,
    activo BOOLEAN DEFAULT TRUE,
    fecha_creacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (rol_id) REFERENCES rol(rol_id)
);

CREATE TABLE opciones_envio (
    opciones_id INT AUTO_INCREMENT PRIMARY KEY,
    dias INT NOT NULL,
    precio DECIMAL(10,2) NOT NULL
);

CREATE TABLE producto (
    producto_id INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(50) NOT NULL,
    descripcion TEXT,
    precio DECIMAL(10,2) NOT NULL,
    stock INT NOT NULL,
    activo BOOLEAN DEFAULT TRUE,
    fecha_creacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CHECK (stock >= 0 AND precio >= 0),
    usuario_id INT NOT NULL,
    FOREIGN KEY (usuario_id) REFERENCES usuario(usuario_id)
);

CREATE TABLE carrito (
    carrito_id INT AUTO_INCREMENT PRIMARY KEY,
    usuario_id INT NOT NULL,
    estado ENUM('ACTIVO','CERRADO') DEFAULT 'ACTIVO',
    fecha_creacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (usuario_id) REFERENCES usuario(usuario_id)
);

CREATE TABLE producto_carrito (
    carrito_id INT NOT NULL,
    producto_id INT NOT NULL,
    cantidad INT NOT NULL,

    PRIMARY KEY (carrito_id, producto_id),

    FOREIGN KEY (carrito_id) REFERENCES carrito(carrito_id),
    FOREIGN KEY (producto_id) REFERENCES producto(producto_id),
    CHECK (cantidad >=0)
);

CREATE TABLE pedido (
    pedido_id INT AUTO_INCREMENT PRIMARY KEY,
    usuario_id INT NOT NULL,
    opcion_envio_id INT NOT NULL,
    total DECIMAL(10,2) NOT NULL,
    estado ENUM('PENDIENTE','PAGADO','ENVIADO','ENTREGADO','CANCELADO') DEFAULT 'PENDIENTE',
    fecha TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (usuario_id) REFERENCES usuario(usuario_id),
    FOREIGN KEY (opcion_envio_id) REFERENCES opciones_envio(opciones_id),
    CHECK (total >= 0)
);

CREATE TABLE pedido_detalle (
    pedido_id INT NOT NULL,
    producto_id INT NOT NULL,
    precio_unitario DECIMAL(10,2) NOT NULL,
    cantidad INT NOT NULL,

    PRIMARY KEY (pedido_id, producto_id),

    FOREIGN KEY (pedido_id) REFERENCES pedido(pedido_id),
    FOREIGN KEY (producto_id) REFERENCES producto(producto_id),
    CHECK (cantidad >= 0)
);

INSERT INTO rol (nombre) VALUES
('Invitado'),
('Usuario'),
('Vendedor'),
('Desarrollador');

INSERT INTO usuario
(dni, nombre, apellido, direccion, codigo_postal, telefono, email, password_hash, rol_id)
VALUES
('12345678Z', 'Juan', 'Perez', 'Calle Mayor 10', '28001', '600123456', 'juan@email.com',
'$2b$12$hashfalsojuan', 2),

('87654321X', 'Ana', 'García', 'Av. España 25', '28002', '600654321', 'ana@email.com',
'$2b$12$hashfalsoana', 2),

('11223344B', 'Carlos', 'Admin', 'Calle Backend 1', '28003', '699999999', 'admin@email.com',
'$2b$12$hashfalsoadmin', 4),

('96857432Q', 'Pepe', 'Hardware', 'Calle Hardware 5', '28021', '649994854',
'pepe.hardware@hardware.es', '$2b$12$g35tBhdcV9EKnMnBG8akAet8A0dCwizkK7twIs9UkZtTs0aFCtyNK', 3);

INSERT INTO usuario (nombre, apellido, email, password_hash, rol_id) VALUES
('Invitado', 'Invitado', 'invitado@invitado.com', '$2a$12$m2Lp.51Gk2eowN4z5Cxz3.kZiMhcXee3onV81uKgMCJIEQ.DquXfy', 1);
INSERT INTO opciones_envio (dias, precio) VALUES
(1, 9.99),
(3, 4.99),
(7, 0.00);

INSERT INTO producto (nombre, descripcion, precio, stock, usuario_id) VALUES
('Portátil Gaming', 'Portátil potente para gaming y desarrollo', 1299.99, 10, 4),

('Ratón Inalámbrico', 'Ratón ergonómico con batería recargable', 29.99, 50, 4),

('Teclado Mecánico', 'Teclado mecánico RGB', 89.99, 30, 4);

INSERT INTO carrito (usuario_id, estado) VALUES
(1, 'ACTIVO'),
(2, 'ACTIVO');

INSERT INTO producto_carrito (carrito_id, producto_id, cantidad) VALUES
(1, 1, 1),
(1, 2, 2),
(2, 3, 1);

INSERT INTO pedido (usuario_id, opcion_envio_id, total, estado) VALUES
(1, 2, 1359.97, 'PAGADO'),
(2, 3, 89.99, 'PENDIENTE');

INSERT INTO pedido_detalle (pedido_id, producto_id, precio_unitario, cantidad) VALUES
(1, 1, 1299.99, 1),
(1, 2, 29.99, 2),
(2, 3, 89.99, 1);